# date-holidays Import Tooling

This directory will contain tooling that imports supported subsets of
`commenthol/date-holidays` YAML data into OpenHolidaysKit's intermediate
representation.

Unsupported rules must be reported explicitly and must not be silently ignored.
